<div id="ctl00_menu" class="menu mt10 v2MenuNav">
    <ol>
        <li><a href="/Home/Index">Home</a></li>
        @*@if (!User.IsInRole("Author"))
            {
                <li id="ctl00_menuRequests">
                    <a href="/Requests/Index">Requests</a>
                </li>
            }
            @if (!User.IsInRole("SalesRep"))
            {
                <li id="ctl00_menuDecisions">
                    <a href="/Decisions/Index">Decisions</a>
                </li>
            }*@
        @if (Request.IsAuthenticated && !User.IsInRole("Author"))
        {
            <li id="ctl00_menuCalendar">
                <a href="/Author/Index">Authors</a>
            </li>
        }
        @if (Request.IsAuthenticated && (User.IsInRole("Admin") || User.IsInRole("SuperAdmin")))
        {
            <li id="ctl00_menuAdmin">
                <a href="/Admin/Index">Admin</a>
            </li>
        }
        <li><a href="#" class="introduction">Help</a></li>
        <li>
            <style>
                #logoutForm { display: inline; }
            </style>
            @using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm" }))
            {
                @Html.AntiForgeryToken()

                <a href="javascript:document.getElementById('logoutForm').submit()">Logout</a>

            }
        </li>

    </ol>
</div>
<div class="clear"></div>